# Vendored external sources + vcs-imported siblings (pulled via vcs import)
external/
# fm-docker compose overlays, imported by run.sh's container path
docker/

# colcon build artifacts (run.sh native path builds in place)
build/
install/
log/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Node (foxglove panel)
node_modules/
dist/

# MuJoCo runtime log
MUJOCO_LOG.TXT

# Planning artifacts (working docs, not committed history)
plans/

# Editor / OS
.vscode/
.idea/
*.swp
.DS_Store
